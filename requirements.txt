# Direct dependencies only; everything else comes in with these.

# Embedding: local ONNX model, no network calls.
numpy
onnxruntime
tokenizers

# The `hf` CLI, for the one download in step 1 of the README. It arrives
# anyway as a dependency of tokenizers; it is named here because step 1 calls
# it directly, and a transitive dependency that drops out takes the install
# step with it.
huggingface_hub

# Frontmatter.
PyYAML

# Optional: exact token counts. Without it, count_tokens falls back to an
# estimate at 4 chars/token and says so in what it returns.
# tiktoken
