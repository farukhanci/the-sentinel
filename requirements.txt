# Direct dependencies only; everything else comes in with these.

# Embedding: local ONNX model, no network calls.
numpy
onnxruntime
tokenizers

# Frontmatter.
PyYAML

# Optional: exact token counts. Without it, count_tokens falls back to an
# estimate at 4 chars/token and says so in what it returns.
# tiktoken
